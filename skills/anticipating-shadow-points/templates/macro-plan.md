# Macro Plan — {{projeto}}

> ASP Phase 4 output. Companion to `project-charter.md` and `deliverables-register.md`. Submit all three for user approval in Phase 7.

## Context

Why this work is being done — the problem from charter, what prompted it, intended outcome.

## Architecture (overview)

How the solution fits together. Use ASCII or mermaid for diagrams. Name the major components.

## File Inventory (literal paths)

| File | Purpose |
|---|---|
| `<absolute/path>` | <one-line purpose> |

## Dependencies / Reused utilities

Existing functions, libraries, services that this work builds on. Cite file paths + symbols.

## Verification end-to-end

How to prove the change works after execution:

1. Run `<command>`; expect `<output>`.
2. Inspect `<file>`; expect `<state>`.
3. Run test suite `<command>`; expect `<count>` pass.

## Shadow points + mitigations (from Phase 2)

| ID | Shadow point | Category | Mitigation | Status |
|---|---|---|---|---|
| SP01 | <name> | spec/data/integration/concurrency/security/observability/op-ex/contract/time-tz/cost | <action or accept-risk + rationale> | mitigated / accepted |

## Out-of-scope (explicit)

What this plan deliberately does NOT cover, and why.

## Validator verdict

| Round | Verdict | Issues |
|---|---|---|
| 1 | APPROVE / REVISE | |
| 2 (if any) | | |
| 3 (if any) | | |
| advisor() | (optional in --paranoid) | |
